import React from 'react'

const header = () => {
  return (
    <header className='fixed backdrop-blur-md z-50 inset-x-0 top-0 flex items-center justify-between'>
    <navLink to={"/"} className="flex items-center justify-center gap-4">
      <img src={} alt="" />
    </navLink>
    </header>
  )
}

export default header
